{
  "name": "zebrash-examples",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-side example app: render every ZPL fixture in the repo.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "zebrash": "file:.."
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
